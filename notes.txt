What is Node JS -> Node JS is 

Javascript --> Runs only under in Browser because every browser has engine that is Javascript engine to exicute code.
Every browser has own engine Most popular is V8 Engine that is used to run code in JS.
A scientist embeded (V8 engine with C++ ) And this is NodeJS.
Now we can run JS outside of the browser.
Jacascript can talk to native machine because of C++.
You can create webservers in Javascript Language.
NodeJS is just Runtime Environment for javascript.

NPM --> Node Package Manager

Node JS Architecture:
client make request to server
request first queue in event queue
if user 1 req -> event queue
user 2  -> event queue 
after that req goes to event loop 
FIFO 
pick req it is of two types:
Blocking operations (SYNC)
Non - Blocking operations (ASYNC)

if it is Non-Blocking:
it is simple req then it process and response to the user.

if it is Blocking Operation:
then it goes to thread pool.
thread pool is a pool in which there are thread.
thread means workers (threads are limited)
then each thread assigning to do task

if no thread free then task will wait until thread become free. 
so always run non-blocking operations. [Async]

Default thread pool size = 4 {max req = 4}
increase up to your cores in cpu .

HTTP Methods: 
1.HTTP GET :- When you want to get some data from the server

2.HTTP POST :- When you want to send and mutate some data in server

3.HTTP PUT :-
4.HTTP PATCH :-
5.DELETE PATCH:-

//Versions 
4.18.3
 1st part -> 4
 2nd part -> 18
 3rd part -> 3

 3rd Part (Last part) - Minor Fixes (optional)
 Latest -> 4.28.5

2nd Part - Recommended Bug Fixes(security fix)

1st Part - Major Release (Breaking update)

if use npm i express to download -> it download latest Version
or if i used npm i ecpress@.... version name -> it installs that specify version

